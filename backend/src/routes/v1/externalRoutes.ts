import { Router } from 'express';

const router = Router();

/**
 * @summary External (public) API routes
 * @description Routes accessible without authentication
 *
 * Examples:
 * - /api/v1/external/public/...
 * - /api/v1/external/security/...
 */

/**
 * @summary Public routes placeholder
 * @description Add public route imports here as features are implemented
 */

export default router;
