import { Router } from 'express';

const router = Router();

/**
 * @summary Internal (authenticated) API routes
 * @description Routes requiring authentication
 *
 * Examples:
 * - /api/v1/internal/menu/...
 * - /api/v1/internal/location/...
 */

/**
 * @summary Internal routes placeholder
 * @description Add authenticated route imports here as features are implemented
 */

export default router;
